export declare class Observer<Value> {
    private _current;
    private _emitter;
    constructor(value?: Value);
    get current(): Value;
    set current(value: Value);
    subscribe(callback: (value: Value) => void): void;
    unsubscribe(callback?: (value: Value) => void): void;
}
